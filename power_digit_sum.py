print(sum(map(int, str(2**1000))))
# str(2**1000) separates the digits of that number into a string of numbers.map(int, string) gives an integer to every digit so that it is now it's own number and compiles all the integers into a list so they can be summed up.
